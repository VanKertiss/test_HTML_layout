import HS from './Title.module.css';

const Title = () => {
    return (
        <div className={HS.container}>
            <div className={HS.titleText}>Sign up</div>
        </div>
    )
};

export default Title;